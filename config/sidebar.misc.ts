import type { SidebarsConfig } from "@docusaurus/plugin-content-docs";

const misc: SidebarsConfig = {
  primary: ["README", "java-install", "downloads-api", "hangar-publishing", "assets", "contact", "mcdev-project-setup"],
};

export = misc;
