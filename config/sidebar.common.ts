import type { SidebarsConfig } from "@docusaurus/plugin-content-docs";

const common: SidebarsConfig = {
  primary: ["common", "java-install", "downloads-api", "assets", "contact"],
};

export = common;
