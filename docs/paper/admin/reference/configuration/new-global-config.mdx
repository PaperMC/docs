# New Global Configuration

import React from 'react';
import Config from '../../../../../src/components/Config';
import yaml from 'js-yaml';

<Config
    data={yaml.load(`
chunk-loading:
  autoconfig-send-distance:
    default: 'true'
    description: Whether to use the client's view distance for the chunk send distance
      of the
  enable-frustum-priority:
    default: 'false'
    description: Whether to attempt to load chunks in front of the player before loading
      chunks to
  global-max-chunk-load-rate:
    default: '-1.0'
    description: The maximum number of chunks loaded per second for the whole server.
      Takes
  global-max-chunk-send-rate:
    default: '-1.0'
    description: The maximum number of chunks sent per second for the entire server.
      This may help
  global-max-concurrent-loads:
    default: '500.0'
    description: The maximum number of chunk loads processed for the whole server
      one time. This
  max-concurrent-sends:
    default: '2'
    description: The maximum number of chunks that will be queued to send at any one
      time. Lower
  min-load-radius:
    default: '2'
    description: The radius of chunks around a player that are not throttled for chunk
      loading.
  player-max-chunk-load-rate:
    default: '-1.0'
    description: The maximum number of chunks loaded per second per player.
  player-max-concurrent-loads:
    default: '20.0'
    description: The maximum number of chunk loads processed per player at one time.
  target-player-chunk-send-rate:
    default: '100.0'
    description: The maximum number of chunks sent to an individual player within
      one second.
chunk-system:
  gen-parallelism:
    default: default
    description: Sets whether the server should use parallel chunk generation. The
      default value
  io-threads:
    default: '-1'
    description: Sets the number of threads to be used for read and write operations
      with chunks.
  worker-threads:
    default: '-1'
    description: Sets the number of threads to be used for parallel chunk generation.
      If any value below
collisions:
  enable-player-collisions:
    default: 'true'
    description: Sets whether the server should allow players to collide with one
      another. This
  send-full-pos-for-hard-colliding-entities:
    default: 'true'
    description: Collisions with boats and minecarts are often subject to client/server
commands:
  fix-target-selector-tag-completion:
    default: 'true'
    description: Workaround for a client bug preventing entity type tag suggestions
      from
  suggest-player-names-when-null-tab-completions:
    default: 'true'
    description: Instructs the server to return a list of players when tab-completing
      when there
  time-command-affects-all-worlds:
    default: 'false'
    description: Whether the /time command should act on all worlds or just the sender's
      current
console:
  enable-brigadier-completions:
    default: 'true'
    description: Enables Mojang's Brigadier (advanced) command completions in the
      server console.
  enable-brigadier-highlighting:
    default: 'true'
    description: Enables Mojang's Brigadier highlighting in the server console.
  has-all-permissions:
    default: 'false'
    description: Whether the console command sender has all permissions.
item-validation:
  display-name:
    default: '8192'
    description: The maximum length of an item's display name in characters.
  lore-line:
    default: '8192'
    description: The maximum length of a lore line in characters.
  resolve-selectors-in-books:
    default: 'false'
  book:
    author:
      default: '8192'
      description: The maximum length of a book's author in characters.
    page:
      default: '16384'
      description: The maximum length of a book's page in characters.
    title:
      default: '8192'
      description: The maximum length of a book's title in characters.
  book-size:
    page-max:
      default: '2560'
      description: The max number of bytes a single page in a book can contribute
        to the allowed
    total-multiplier:
      default: '0.98'
      description: Each page has this multiple of bytes from the last page as its
        contribution to
logging:
  deobfuscate-stacktraces:
    default: 'true'
    description: Whether to remap Spigot mapped stacktraces to Mojang mappings in
      logging. Has no
  log-player-ip-addresses:
    default: 'true'
    description: Whether player IP addresses should be logged by the server. This
      does not impact
  use-rgb-for-named-text-colors:
    default: 'true'
    description: Whether named ANSI colors should be logged with RGB codes.
messages:
  no-permission:
    description: Default message sent to players when they have insufficient permissions
      for an
  use-display-name-in-quit-message:
    default: 'false'
    description: Whether the server should use the player's display name (set by plugins)
      or
  kick:
    authentication-servers-down:
      default: <lang:multiplayer.disconnect.authservers_down>
      description: Message sent to players when Mojang's authentication servers are
        unreachable.
    connection-throttle:
      default: Connection throttled! Please wait before reconnecting.
    flying-player:
      default: <lang:multiplayer.disconnect.flying>
      description: Message sent to players who are detected flying.
    flying-vehicle:
      default: <lang:multiplayer.disconnect.flying>
      description: Message sent to players who are detected riding a flying vehicle.
misc:
  fix-entity-position-desync:
    default: 'true'
    description: Causes the server to lose the same precision that the client does
      for entities
  lag-compensate-block-breaking:
    default: 'true'
    description: Whether the server should use time or TPS to determine block break
      duration. The
  load-permissions-yml-before-plugins:
    default: 'true'
    description: Loads bukkit's permission.yml file before plugins, allowing them
      to check
  max-joins-per-tick:
    default: '3'
    description: Sets the maximum amount of players that may join the server in a
      single tick. If
  region-file-cache-size:
    default: '256'
    description: The maximum size of the region file cache.
  use-alternative-luck-formula:
    default: 'false'
    description: Use an
  use-dimension-type-for-custom-spawners:
    default: 'false'
    description: Whether phantoms, wandering traders, etc. should be able to spawn
      in custom
packet-limiter:
  all-packets:
    action:
      default: KICK
      description: The action to take once the limit has been violated. Possible values
        are DROP
    interval:
      default: '7.0'
      description: The interval, in seconds, for which max-packet-rate should apply.
    max-packet-rate:
      default: '500.0'
      description: The number of packets allowed per player within the interval.
  kick-message:
    default: <red><lang:disconnect.exceeded_packet_rate>
    description: The message players are kicked with for sending too many packets.
  overrides:
    default: ServerboundPlaceRecipePacket
    description: Override the global configuration for any individual named packet.
      You can find
player-auto-save:
  max-per-tick:
    default: '-1'
    description: How many players should be saved at most in a single tick. A value
      of -1 will
  rate:
    default: '-1'
    description: How often player data should be saved in ticks. A value of -1 will
      use
proxies:
  proxy-protocol:
    default: 'false'
    description: Whether the server should process
  bungee-cord:
    online-mode:
      default: 'true'
  velocity:
    enabled:
      default: 'false'
      description: Whether the server should accept Velocity Modern Forwarding.
    online-mode:
      default: 'false'
      description: Instructs the server how to handle player UUIDs and data when behind
        Velocity.
    secret:
      default: ' '
      description: The secret string that is shared by your Velocity proxy and this
        server. This
scoreboards:
  save-empty-scoreboard-teams:
    default: 'false'
    description: Some scoreboard plugins leave hundreds of empty scoreboard teams
      around,
  track-plugin-scoreboards:
    default: 'false'
    description: Whether the server should track plugin scoreboards with only dummy
      objectives.
spam-limiter:
  incoming-packet-threshold:
    default: '300'
    description: Sets the threshold at which the server will consider incoming packets
      spam and
  recipe-spam-increment:
    default: '1'
    description: The number that the recipe spam counter increases by when a player
      presses a
  recipe-spam-limit:
    default: '20'
    description: The number that the recipe spam counter can reach until the server
      kicks the
  tab-spam-increment:
    default: '1'
    description: The number that the internal tab spam counter increases by when a
      player presses
  tab-spam-limit:
    default: '500'
    description: The number that the internal tab spam counter can reach until the
      server kicks
timings:
  enabled:
    default: 'true'
    description: Controls the global enable state of Timings.
  hidden-config-entries:
    description: Configuration entries to hide in Timings reports.
  history-interval:
    default: '300'
    description: The interval in seconds between individual points in the Timings
      report.
  history-length:
    default: '3600'
    description: The total amount of data to keep for a single report. This value
      is validated
  server-name:
    default: Unknown Server
    description: Instructs Timings on what to put in for the server name.
  server-name-privacy:
    default: 'false'
    description: Instructs Timings to hide server name information in reports.
  url:
    default: https://timings.aikar.co
    description: Specifies the URL of the [Timings Viewer](https://github.com/aikar/timings)
  verbose:
    default: 'true'
    description: Instructs Timings to provide more specific information in its reports.
      For
unsupported-settings:
  allow-headless-pistons:
    default: 'false'
    description: Whether the server should allow the creation of headless pistons.
      These are often
  allow-permanent-block-break-exploits:
    default: 'false'
    description: Whether unbreakable blocks can be broken with vanilla exploits. This
      includes
  allow-piston-duplication:
    default: 'false'
    description: Whether to allow duplication of TNT, carpets, and rails. This does
      not control
  perform-username-validation:
    default: 'true'
watchdog:
  early-warning-delay:
    default: '10000'
    description: The number of milliseconds before the watchdog thread starts printing
      thread
  early-warning-every:
    default: '5000'
    description: The interval in milliseconds between printed thread dumps while the
      server is

`)}
/>

