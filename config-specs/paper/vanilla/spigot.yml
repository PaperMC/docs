world-settings:
  default:
    entity-activation-range:
      animals:
        default: "0"
      monsters:
        default: "0"
      raiders:
        default: "0"
      misc:
        default: "0"
      water:
        default: "0"
      villagers:
        default: "0"
      flying-monsters:
        default: "0"
    max-tnt-per-tick:
      default: "-1"
